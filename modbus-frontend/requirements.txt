# Dipendenze runtime di modbus-frontend
# Dedotte dagli import reali in modbus-frontend/app.py
#
# NOTA: il repository non conteneva un requirements.txt per questo componente.
# gunicorn non compare in nessun import Python ma è richiesto perché
# service_definition/modbus_frontend.service lo usa come server WSGI di
# produzione (ExecStart=... gunicorn ... app:app). Senza questa riga il
# servizio fallirebbe l'avvio con "gunicorn: command not found".

flask==3.1.3
flask-cors==6.0.5
mysql-connector-python==9.7.0
gunicorn==26.0.0